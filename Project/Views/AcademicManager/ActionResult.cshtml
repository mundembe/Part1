@{
    ViewData["Title"] = "Action Result";
}

<div class="container">
    <h2>Action Result</h2>
    <p>@ViewData["Message"]</p>
    <a href="@Url.Action("Index", "AcademicManager")" class="btn btn-primary">Back to Dashboard</a>
</div>
